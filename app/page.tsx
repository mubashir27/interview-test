import HomeView from "@/src/sections/home/view/HomeView";

// metadata function is used for the tab name.
export const metadata = {
  title: "dashboard page",
};

export default function HomePage() {
  return <HomeView />;
}
